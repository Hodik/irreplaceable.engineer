---
// Reusable branding component
---

<div class="branding">
  <span class="accent">Irreplaceable Backend Engineer</span>
</div>

<style>
  .branding {
    text-align: center;
    margin: 0;
  }

  .text {
    font-size: clamp(1rem, 2vw, 1.25rem);
    font-weight: 600;
    color: var(--text);
    letter-spacing: -0.01em;
  }

  .accent {
    font-size: clamp(1rem, 2vw, 1.25rem));
    font-weight: bold;
  }
</style>
